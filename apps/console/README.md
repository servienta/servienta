# Console (console.servienta.com)

Customer-facing management, settings, and statistics. One Go service in Docker listening on :80,
serving the REST API and the Vue 3 SPA (decision D11). Not started — see `docs/roadmap.md`.
