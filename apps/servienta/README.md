# Servienta engine

The test-services data plane: protocol receivers, five-transport file server, versioned HTTP
contract. Go, Docker compose — the delivery's only container (decisions D6, D12). Scope:
`docs/requirements.md`. Code starts when the phase 0 inputs are closed — see `docs/roadmap.md`.
