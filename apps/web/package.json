{
  "name": "@servienta/web",
  "private": true,
  "scripts": {
    "build": "node assets/gen-images.mjs && tailwindcss -i src/styles.css -o public/styles.css --minify",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
