packages:
  - apps/*
  - packages/*

allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  workerd: true

# Cloudflare publishes type packages daily; exempt them from the release-age policy.
minimumReleaseAgeExclude:
  - "@cloudflare/workers-types"
